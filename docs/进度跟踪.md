# 项目进度跟踪
# 【负责人】项目负责人

**维护人**：项目负责人
**更新日期**：2026-06-04

---

## 总体进度

| 阶段 | 进度 | 状态 |
|------|:----:|:----:|
| 架构设计 | 100% | ✅ 已完成 |
| 框架搭建 | 100% | ✅ 已完成 |
| 开发文档 | 100% | ✅ 已完成 |
| 数据管理模块 | 100% | ✅ 已完成（含 export + upload + SQLite 持久化） |
| 数据清洗模块 | 100% | ✅ 已完成（缺失值处理 + IQR 异常检测，PR #12 由 sgase233 提交） |
| 可视化模块 | 100% | ✅ 已完成（Plotly 交互式图表，PR #9 由 chensac 提交） |
| 分析功能模块 | 0% | ⏳ 待开发（后端桩代码） |
| 用户认证模块 | 100% | ✅ 已完成（注册+登录/登出+会话管理） |
| Web 界面模块 | 100% | ✅ 已完成 |
| 测试框架 | 100% | ✅ 基础设施就绪（7 个文件，48 通过 + 41 跳过） |
| 集成测试 | 0% | ⏳ 待其他模块实现后执行 |
| 项目交付 | 0% | ⏳ 待开始 |

---

## 各模块详情

### 数据管理模块

| 项目 | 内容 |
|------|------|
| **负责人** | 数据管理模块开发人员（`yxxx0822` 提交 PR #4） |
| **状态** | ✅ 已完成 |
| **涉及文件** | `services/data_service.py`, `routes/upload.py`, `routes/export.py`, `static/js/upload.js`, `repositories/file_repo.py` |
| **当前工作** | `_parse_file()` 已实现（CSV 多编码自动探测 + Excel openpyxl 解析）；`POST /upload` 路由已实现（文件校验 + 异常处理）；`GET /export` 正常（CSV BOM + XLSX）；NaN 在预览中正确转为 JSON null |
| **关键变更** | 存储层从 FileRepository（CSV 文件）升级为 SQLiteRepository（SQLite + Parquet），重启不丢数据 |
| **阻塞问题** | 无 |

### 数据清洗模块

| 项目 | 内容 |
|------|------|
| **负责人** | 数据清洗模块开发人员（`sgase233` 提交 PR #12） |
| **状态** | ✅ 已完成 |
| **涉及文件** | `services/clean_service.py`, `routes/clean.py`, `static/js/clean.js` |
| **当前工作** | 缺失值处理（均值/中位数填充、删除行）+ IQR 异常检测已实现；前端 `clean.js` 此前已由 Web 界面模块完成 |
| **阻塞问题** | 无 |

### 可视化模块

| 项目 | 内容 |
|------|------|
| **负责人** | 可视化模块开发人员（`chensac` 提交 PR #9） |
| **状态** | ✅ 已完成 |
| **涉及文件** | `services/visualize_service.py`, `routes/plot.py`, `static/js/plot.js` |
| **当前工作** | Plotly 交互式图表（散点图/折线图/柱状图/饼图），返回 Plotly JSON 供前端 Plotly.js 渲染 |
| **阻塞问题** | 无 |

### 分析功能模块

| 项目 | 内容 |
|------|------|
| **负责人** | 分析功能模块开发人员 |
| **状态** | ⏳ 待开发 |
| **涉及文件** | `services/analyze_service.py`, `routes/analyze.py`, `static/js/analyze.js` |
| **当前工作** | `analyze.js` 前端逻辑已完成（K-Means + 线性回归参数配置）；后端为桩代码 |
| **阻塞问题** | 无 |

### Web 界面模块

| 项目 | 内容 |
|------|------|
| **负责人** | Web 界面模块开发人员 |
| **状态** | ✅ 已完成 |
| **涉及文件** | `templates/index.html`, `static/css/style.css`, `static/js/app.js`, `static/js/upload.js`, `static/js/clean.js`, `static/js/plot.js`, `static/js/analyze.js` |
| **当前工作** | 全部已完成 |
| **阻塞问题** | 无 |

### 测试框架

| 项目 | 内容 |
|------|------|
| **负责人** | 项目负责人 |
| **状态** | ✅ 已完成（基础设施） |
| **涉及文件** | `test/` 目录，7 个文件 |
| **当前工作** | 48 个测试通过 + 41 个测试模板（标记 skip，待模块实现后启用） |
| **覆盖范围** | FileRepository / SQLiteRepository / DataService export / 值对象 / 路由错误处理 / 端到端流程 |

---

## 待办事项

- [x] 创建 `dev` 分支，各模块从 dev 拉取功能分支
- [x] Web 界面模块开发完成（`feat/Web界面` 已合并到 `dev`）
- [x] 接口确认：前端使用 Plotly 方案
- [x] 数据管理模块：文件上传（PR #4 合并 → 已完成）
- [x] 框架搭建、通用组件（全部完成）
- [x] 文档：7 份开发文档 + 规范 + 集成指南 + 进度跟踪
- [x] SQLite 持久化存储（本次完成）
- [x] 测试框架：7 个文件，48 通过
- [x] 用户认证模块：注册、登录/登出、会话管理
- [x] 数据清洗模块：缺失值处理 + IQR 异常检测（PR #12 合并）
- [x] 可视化模块：Plotly 交互式图表（PR #9 合并）
- [ ] 分析功能模块：实现后端服务与路由
- [ ] 集成测试：端到端验证全流程（含认证态）
- [ ] 项目交付

---

## Git 分支状态

```
main ← dev（当前活跃）
         ├── feat/Web界面（已合并）
         ├── feat/data-mgt-management（已合并）
         ├── feat/可视化 → PR #9（已合并，Plotly）
         ├── respositories（已合并）
         ├── login-backend → PR #8（已合并）
         └── PR #12（数据清洗，已合并）
```

## 当前运行时状态

| 接口 | 方法 | 状态 | 说明 |
|------|------|:----:|------|
| `GET /` | 首页 | ✅ | UI 正常 |
| `POST /upload` | 上传 | ✅ | 支持 CSV（UTF-8/GBK 自动探测）+ XLSX |
| `POST /clean` | 清洗 | ✅ | 缺失值处理（均值/中位数/删除）+ IQR 异常检测 |
| `POST /plot` | 可视化 | ✅ | Plotly 散点图/折线图/柱状图/饼图 |
| `POST /analyze` | 分析 | ⏳ | 返回 500 NotImplementedError |
| `GET /export` | 导出 | ✅ | 支持 CSV（BOM）+ XLSX，含错误处理 |
| `POST /api/register` | 注册 | ✅ | 密码 hash 存储，重复用户名校验 |
| `POST /api/login` | 登录 | ✅ | session 写入 user_id |
| `POST /api/logout` | 登出 | ✅ | 清除 session |
| `GET /api/me` | 当前用户 | ✅ | 返回用户信息或 401 |
| **存储层** | SQLite | ✅ | `analysis.db`，应用重启数据不丢失 |

## 备注

- 各模块开发文档位于 `docs/1-数据管理模块/开发文档.md` 等对应目录
- 项目负责人手册见 `docs/0-项目负责人/开发文档.md`、`docs/0-项目负责人/工作汇报.md`
- 集成步骤见 `docs/项目集成指南(必看).md`
- 开发规范见 `docs/项目开发规范(必看).md`
- **测试命令**: `python -m pytest test/ -v`
- **测试数据**: 根目录 `test_data_csv.csv`（22 行 × 7 列，未入 git，需手动复制）
