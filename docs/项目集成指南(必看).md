# 项目集成指南
# 【负责人】项目负责人

---

## 一、集成总览

```
                          ┌─────────────────────────┐
                          │     app.py (应用工厂)     │
                          │   项目负责人 负责组装所有依赖     │
                          └──────┬──────────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │                      │                      │
          ▼                      ▼                      ▼
   ┌─────────────┐      ┌──────────────┐      ┌──────────────┐
   │ 控制层路由   │      │ 控制层路由    │      │ 控制层路由    │
   │ upload/     │      │ clean/       │      │ plot/        │
   │ export      │      │              │      │              │
   └──────┬──────┘      └──────┬───────┘      └──────┬───────┘
          │                     │                     │
          ▼                     ▼                     ▼
   ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
   │ DataService  │    │ CleanService │    │VisualizeSvc  │
   │ (数据管理)    │    │ (数据清洗)    │    │ (可视化)      │
   └──────┬──────┘    └──────┬───────┘    └──────┬───────┘
          │                     │                     │
          └─────────────────────┼─────────────────────┘
                                │
                    ┌───────────▼───────────┐
                    │  DataRepository 抽象   │
                    │  (注入到所有 Service)  │
                    └───────────┬───────────┘
                                │
                    ┌───────────▼───────────┐
                    │   FileRepository      │
                    │   (CSV 临时文件)       │
                    └───────────────────────┘
```

---

## 二、集成步骤

### 第 1 步：项目负责人 完成通用组件

- [x] `config.py` — 全局配置
- [x] `value_objects.py` — `DatasetRef` 值对象
- [x] `repositories/base.py` — `DataRepository` 抽象接口
- [x] `repositories/file_repo.py` — `FileRepository` 实现
- [x] `routes/__init__.py` — 蓝图注册函数
- [x] `services/__init__.py` — Service 导出
- [ ] `app.py` — **创建依赖注入链**（见下方第 5 步）

### 第 1.5 步：各开发人员启用 Git 提交规范

**克隆项目后第一件事，运行以下命令：**

```bash
git config core.hooksPath githooks/
```

设置后 commit message 必须符合 `<type>: <描述>` 格式，否则会被拒绝提交。这是项目统一的提交规范，每人必须设置。

### 第 2 步：各模块并行开发

各模块开发人员在自己的功能分支上并行工作：

| 模块 | 后端 Service | 后端 Route | 前端 JS |
|------|-------------|-----------|---------|
| 数据管理 | `data_service.py` | `upload.py`, `export.py` | `upload.js` |
| 数据清洗 | `clean_service.py` | `clean.py` | `clean.js` |
| 可视化 | `visualize_service.py` | `plot.py` | `plot.js` |
| 分析功能 | `analyze_service.py` | `analyze.py` | `analyze.js` |
| Web 界面 | — | — | `app.js` + `index.html` + `style.css` |

### 第 3 步：PR 合入 dev 分支

各模块完成开发后，向 `dev` 分支发起 Pull Request。

### 第 4 步：项目负责人 集成测试

1. 拉取所有模块的变更到 `dev`
2. 验证依赖注入链
3. 执行端到端测试

### 第 5 步：项目负责人 完成依赖注入

`app.py` 的 `create_app()` 中已注入以下依赖链：

```python
from repositories import FileRepository
from services import DataService, CleanService, VisualizeService, AnalyzeService

repo = FileRepository()
app.data_service = DataService(repo)
app.clean_service = CleanService(repo)
app.visualize_service = VisualizeService(repo)
app.analyze_service = AnalyzeService(repo)
```

---

## 三、各模块接口依赖关系矩阵

| 调用方 \ 被调用方 | DataService | CleanService | VisualizeService | AnalyzeService | FileRepository |
|------------------|-------------|--------------|-----------------|---------------|----------------|
| `upload.py` 路由 | 调用 `upload()` | — | — | — | — |
| `export.py` 路由 | 调用 `.repo` | — | — | — | 间接调用 |
| `clean.py` 路由 | — | 调用 `clean()` | — | — | — |
| `plot.py` 路由 | — | — | 调用 `generate_plot()` | — | — |
| `analyze.py` 路由 | — | — | — | 调用 `analyze()` | — |
| `DataService` | — | — | — | — | 调用 `save/load/delete` |
| `CleanService` | — | — | — | — | 调用 `load/save` |
| `VisualizeService` | — | — | — | — | 调用 `load` |
| `AnalyzeService` | — | — | — | — | 调用 `load` |

---

## 四、集成验证清单

### 4.1 环境检查

- [ ] `pip install -r requirements.txt` 成功
- [ ] `python app.py` 启动无报错
- [ ] 访问 http://localhost:5000 返回页面

### 4.2 数据管理模块验证

- [ ] 上传 `.csv` 文件成功，返回列名、预览、行列数
- [ ] 上传 `.xlsx` 文件成功
- [ ] 上传非支持格式返回 400 错误
- [ ] 导出 CSV 文件内容正确
- [ ] 导出 Excel 文件可正常打开

### 4.3 数据清洗模块验证

- [ ] 均值填充正确执行
- [ ] 中位数填充正确执行
- [ ] 删除行策略正确执行
- [ ] IQR 异常值检测正确报告和移除
- [ ] 原始数据集不受清洗影响

### 4.4 可视化模块验证

- [ ] 散点图正确生成
- [ ] 折线图正确生成
- [ ] 柱状图正确生成
- [ ] 非数值列选择时报错
- [ ] 图表显示标题和坐标轴标签

### 4.5 分析功能模块验证

- [ ] K-Means 聚类返回 labels/centers/inertia
- [ ] K 值参数可调
- [ ] 结果中的 numpy 类型已正确序列化

### 4.6 Web 界面验证

- [ ] 页面初始只显示上传步骤
- [ ] 上传后逐步显示后续步骤
- [ ] 错误信息醒目显示
- [ ] 完整流程（上传→清洗→可视化→分析→导出）无刷新完成

### 4.7 端到端流程

```
完整数据流：
上传 → 预览 → 设置每列清洗策略 → 执行清洗 → 查看报告 →
选择 X/Y 列和图表类型 → 生成图表 → 选择算法和参数 →
执行分析 → 查看结果 → 导出文件
```

- [ ] 每一步的 `dataset_id` 正确传递
- [ ] 清洗/分析后新生成的 `dataset_id` 用于后续操作
- [ ] 导出使用的是最新数据集

---

## 五、常见集成问题

### 5.1 Service 获取不到

**问题**: Route 中 `current_app.xxx_service` 抛出 `AttributeError`

**原因**: `app.py` 中未将 Service 实例挂载到 `app` 上

**解决**: 确保 `create_app()` 中有类似:

```python
app.data_service = DataService(repo)
```

### 5.2 JS 函数未定义

**问题**: 浏览器 Console 报 `Uncaught ReferenceError: xxx is not defined`

**原因**: HTML 中 JS 文件加载顺序有误

**解决**: 各模块 JS 必须在 `app.js` 之前加载，确保 `app.js` 能调用到模块暴露的函数。

### 5.3 Flask 找不到蓝图

**问题**: `Registering blueprint` 时提示模块不存在

**原因**: Route 模块抛出了 `NotImplementedError`（被 import 时触发）

**解决**: 暂用空函数体替换 `raise NotImplementedError`，或由 项目负责人 先注册空蓝图。

### 5.4 跨域问题（开发调试）

如果前端单独启动开发服务器，需要在 Flask 端启用 CORS：

```python
from flask_cors import CORS

def create_app():
    app = Flask(__name__)
    CORS(app)  # 全局启用
    ...
```

---

## 六、各模块开发人员注意事项

### 数据管理模块
- 上传后返回的 `dataset_id` 会作为后续所有操作的标识，确保其正确性
- `export` 路由返回的是文件流，不是 JSON，注意 Content-Type 和 Content-Disposition

### 数据清洗模块
- 清洗产生**新数据集**，返回**新的** `dataset_id`，前端会用新 ID 更新全局状态
- `_handle_missing` 和 `_detect_outliers_iqr` 是内部方法，不需要在 Route 中直接调用

### 可视化模块
- 选择 Matplotlib 或 Plotly 后，需要通知 Web 界面模块，因为：
  - Matplotlib: 前端只需 `<img>` 显示 base64
  - Plotly: 前端需引入 Plotly.js CDN + 调用 `Plotly.newPlot()`

### 分析功能模块
- scikit-learn 返回 numpy 类型，必须转为 Python 原生类型（`.tolist()`, `float()`）

### Web 界面模块
- 负责维护全局 `currentDatasetId` 状态，确保每次请求都携带正确的 ID
- 负责协调各模块 JS 的集成顺序和初始化时机
